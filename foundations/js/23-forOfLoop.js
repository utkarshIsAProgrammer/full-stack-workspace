// ------------------------------------------------------
// with array
const skills = ["frontend", "backend", "authentication", "deployment"];

for (skill of skills) {
	console.log(skill);
}

// with string
const name = "IndieDev";
for (char of name) {
	console.log(char);
}
// ------------------------------------------------------
