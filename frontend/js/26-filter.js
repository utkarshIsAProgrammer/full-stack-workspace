// ------------------------------------------------
// filter method
const numbers = [1, 2, 3, 4, 5];
const even = numbers.filter((number) => {
	return number % 2 === 0;
});
console.log(even);
// ------------------------------------------------
